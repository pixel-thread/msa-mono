# Frontend Development rules baseline

- Components should be modular, placed in `src/features/<feature>/components`.
- Use React Query for API calls.
- Enforce accessibility (a11y) and keyboard navigation.
- Follow tailwind-based theming matching existing pages.
