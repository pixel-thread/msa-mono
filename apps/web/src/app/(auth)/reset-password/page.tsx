import { ResetPasswordPage } from '@feature/auth/pages';

/**
 * Reset password route page.
 * Thin wrapper around the ResetPasswordPage component from the auth feature.
 */
export default function Page() {
  return <ResetPasswordPage />;
}
