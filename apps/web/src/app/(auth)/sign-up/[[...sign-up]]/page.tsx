import { SignUpPage } from '@feature/auth/pages';

/**
 * Sign up route page.
 * Thin wrapper around the SignUpPage component from the auth feature.
 */
export default function Page() {
  return <SignUpPage />;
}
