import { SignInPage } from '@feature/auth/pages';

/**
 * Sign in route page.
 * Thin wrapper around the SignInPage component from the auth feature.
 */
export default function Page() {
  return <SignInPage />;
}
