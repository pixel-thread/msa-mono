// ---- Constants ---------------------------------------------------------------

/** Supported billing cycle options. */
export const BILLING_CYCLES = ['MONTHLY', 'YEARLY'] as const;

/** Supported plan status options. */
export const PLAN_STATUSES = ['ACTIVE', 'INACTIVE'] as const;
